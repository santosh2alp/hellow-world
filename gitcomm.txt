git --version
git config --list
git help config or git add --help
git init
git status
git config --global user.name "Your name"
git config --global user.email "Your email"
git add -A
git commit -m "commit massage"
git log
git log --online
git checkout id or branch name
git tag -a v1.0 -m "commit massage"
git revert id or branch
git reset --hard
git clean -f
git branch